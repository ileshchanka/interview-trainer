name: Проверки и деплой

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      # Kotlin нужен только проверке контента: андроид-задачи в браузере не
      # исполняются, поэтому записанный в JSON вывод — единственный источник
      # вердикта, и он обязан совпадать с настоящим компилятором.
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: 21
      # Сторонний action закреплён по SHA, а не по ветке: `@main` означает
      # «выполнить в нашем CI всё, что автор туда запушит».
      - uses: fwilhe2/setup-kotlin@6fda6d086cbe82e329ceac9937c70d09fe300494 # v2.0
        with:
          version: 2.1.0
      - run: npm run verify:content
      - run: npx ng test --watch=false
      # base-href нужен для GitHub Pages: сайт живёт в подкаталоге репозитория,
      # и без него все ленивые чанки и запросы за контентом уйдут в корень домена.
      - run: npx ng build --base-href /interview-trainer/
      - uses: actions/upload-pages-artifact@v5
        if: github.ref == 'refs/heads/main'
        with:
          path: dist/interview-trainer/browser

  deploy:
    if: github.ref == 'refs/heads/main'
    needs: check
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
